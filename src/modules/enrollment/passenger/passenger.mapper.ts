module.exports = {
    basic: function(_passenger) {
        let passenger;

        passenger = (({
                          requirement
                      }) => ({
            requirement
        }))
        (_passenger);

        return passenger;
    },
};
